# Copy to .env and adjust if needed
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=nexttyproa
DB_USER=root
DB_PASSWORD=123456
# Leave empty to use a random token per run (npm run dev prints it)
AUTH_TOKEN=
VAULT_PATH=
